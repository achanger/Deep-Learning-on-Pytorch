[Folder's description in English]

    1. This folder is used for saving train-step/epoch model parameter.
    2. All checkpoints is divided and put into different folder which is the same as dataset's name in Folder datasets.


[该文件夹的中文说明]
    1. 这个文件夹用于保存训练效果比较好的模型参数。
    2. 所有的检查点(保存的模型)被分割放在和当前训练的数据集名称(datasets下的文件夹名称)相同。


